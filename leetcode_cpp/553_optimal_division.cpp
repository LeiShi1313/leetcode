#include <iostream>
#include <vector>
#include <unordered_map>
#include <string>
#include "utils.h"
using namespace std;

class Solution {
public:
    string optimalDivision(vector<int>& nums) {

    }
};

int main() {
    vector<int> nums;

    nums = {1000, 100, 10, 2};
    cout << Solution().maximumProduct(nums) << endl;
}
